# /etc/youtube-fallback.env é agora um symlink gerido por yt-fallback.
# Execute 'yt-fallback set life' ou 'yt-fallback set bars' para alternar perfis.
# Os perfis de exemplo encontram-se em /etc/youtube-fallback.d/.
#
# Defina pelo menos UM dos seguintes valores antes de iniciar o serviço:
#   - YT_KEY: apenas a chave do stream (sem o URL). A URL é derivada automaticamente.
#   - YT_URL: URL RTMP completo (incluindo ?backup=1/CHAVE).
# Opcionalmente pode ajustar YT_URL_BACKUP caso utilize outra região do YouTube.
#
# Exemplos:
# YT_KEY="CHAVE_DO_STREAM"
# YT_URL_BACKUP="rtmps://b.rtmps.youtube.com/live2"
# YT_URL="rtmps://b.rtmps.youtube.com/live2?backup=1/CHAVE_DO_STREAM"
